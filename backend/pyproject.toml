[project]
name = "bertrum-backend"
version = "0.1.0"
description = "Bertrum — AI-powered email manager API"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
